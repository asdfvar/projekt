Add functionality to assign a numeric value to selected units via ctrl-number. Then, auto-select the assign group via alt-number

remove actions from the map when assigned to the actions list used in society

set the definition of cells in the selection box as cells that are completely contained within the bounds
